#include <stdio.h>
#include "main.h"

/**
 * main - program that prints its name
 * @argc:number of things entered into command line
 * @argv:string value of things entered in command line
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

return (0);

}
